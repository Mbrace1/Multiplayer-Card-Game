using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreManager : MonoBehaviour
{
    // get ref for all cards in col and add to get total col score
    // get ref to total score container

    // do this for both player and opponent side
}
